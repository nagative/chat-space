.upper-message
  .upper-message__user-name
    = message.user.name
  .upper-message__time
    = message.created_at.to_s
.lower-message
  - if message.body.present?
    %p.lower-message__message
      = message.body
  = image_tag message.image.url, class: "message__image" if message.image.present?
